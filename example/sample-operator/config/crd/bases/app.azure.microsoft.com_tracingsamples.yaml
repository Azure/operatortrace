---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tracingsamples.app.azure.microsoft.com
spec:
  group: app.azure.microsoft.com
  names:
    kind: TracingSample
    listKind: TracingSampleList
    plural: tracingsamples
    singular: tracingsample
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: TracingSample is the Schema for the tracingsamples API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TracingSampleSpec defines the desired state of TracingSample.
            properties:
              value:
                description: Value is the incremental counter used to trigger Sample
                  reconciles.
                type: integer
            type: object
          status:
            description: TracingSampleStatus defines the observed state of TracingSample.
            properties:
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
